# forge-demo Roadmap
# Version: Cargo.toml | History: CHANGELOG.md | Config: project.yaml

# ═══════════════════════════════════════════════════════════════════════════════
# COMPLETED
# ═══════════════════════════════════════════════════════════════════════════════

completed:
  - version: "1.0.0"
    date: "2025-12-10"
    summary: "E2E Validation TUI (forge-e2e)"
    deliverables:
      - "Rust CLI with ratatui TUI"
      - "Validates against Gnumeric (ssconvert --recalc)"
      - "48 demo functions, v1.0.0 schema (scalar only)"
      - "Exact match comparison (no tolerance - financial tool)"
      - "Cross-platform binaries via GitHub Releases"

  - version: "1.0.2"
    date: "2025-12-10"
    summary: "Schema Integrity Fix"
    deliverables:
      - "Verified examples/*.yaml are valid v1.0.0 (scalar-only)"
      - "forge v7.2.6: ADR-014 schema versioning, JSON schema validation"
      - "forge v7.2.7: Upgrade command gated to enterprise only"
      - "Git history squashed (4 commits)"

# ═══════════════════════════════════════════════════════════════════════════════
# CURRENT
# ═══════════════════════════════════════════════════════════════════════════════

current:
  version: "1.1.0"
  summary: "Test Coverage Expansion"
  priority: NORMAL
  status: pending
  notes: "Add E2E tests for all 48 demo functions"
  deliverables:
    - "Audit current E2E test coverage"
    - "Add missing function tests"
    - "Ensure all 48 demo functions have E2E validation"

# ═══════════════════════════════════════════════════════════════════════════════
# NEXT
# ═══════════════════════════════════════════════════════════════════════════════

next:
  - version: "1.2.0"
    summary: "TUI Enhancement - Navigation & Details"
    priority: NORMAL
    status: wip
    notes: "First batch of TUI improvements focused on navigation and visibility"
    deliverables:
      - "Scrollable results list with j/k and arrow key navigation"
      - "Tab between panels (results, details, stats)"
      - "Detail pane showing formula + expected + actual for selected test"
      - "Filter view: all / passed / failed (toggle with 1/2/3 keys)"

  - version: "1.3.0"
    summary: "TUI Enhancement - Search & Layout"
    priority: NORMAL
    status: wip
    notes: "Second batch of TUI improvements for power users"
    deliverables:
      - "Search test names with / key"
      - "Side-by-side layout: test list (left) + detail view (right)"
      - "Color-coded function categories (math=blue, text=yellow, etc.)"
      - "Sparkline or mini-chart of pass/fail distribution"

  - version: "1.4.0"
    summary: "TUI Enhancement - Actions & Export"
    priority: NORMAL
    status: wip
    notes: "Third batch of TUI improvements for workflow integration"
    deliverables:
      - "Re-run failed tests with r key"
      - "Expand/collapse by category (math.*, text.*, etc.)"
      - "Save results to JSON/YAML with s key"
      - "Run tests in background thread (non-blocking UI)"

  - version: "1.5.0"
    summary: "Excel Import/Export"
    priority: NORMAL
    status: wip
    notes: "Import test specs from Excel, export results to Excel"
    deliverables:
      - "Import test specs from .xlsx files (formula + expected columns)"
      - "Export test results to .xlsx with pass/fail formatting"
      - "Round-trip: Excel → run tests → Excel report"
      - "Template .xlsx file for easy test authoring"

  - version: "1.6.0"
    summary: "Regression Detection"
    priority: NORMAL
    status: pending
    notes: "Save baseline results, detect regressions on new forge-demo releases"

# ═══════════════════════════════════════════════════════════════════════════════
# BACKLOG
# ═══════════════════════════════════════════════════════════════════════════════

backlog:
  - "GitHub Actions CI integration (run forge-e2e --all on PR)"
  - "Performance benchmarks (rows/sec)"
  - "Excel formula coverage report"
